name: Build Pipeline

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build and Push
    steps:
      - name: git-checkout
        uses: actions/checkout@v3

      - name: Install all dependencies
        run: npm install

      - name: Test
        run: npm run test
        
      - name: Build
        run: npm run build
